version: "3.9"

services:

  intellidocs:

    build: .

    container_name: intellidocs-ai

    ports:
      - "8501:8501"

    volumes:
      - ./data:/app/data
      - ./vector_store:/app/vector_store

    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}

    restart: unless-stopped